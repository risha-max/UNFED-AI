{
  "_comment": "UNFED AI — Guard Relay Node Config. Copy to guard_config.json and adjust.",

  "role": "guard",
  "port": 50060,
  "host": "[::]",
  "advertise": "localhost:50060",
  "registry": "localhost:50050",

  "max_concurrent_relays": 100,
  "relay_timeout_seconds": 120.0,
  "log_connections": false,

  "grpc_max_workers": 10,
  "grpc_max_message_mb": 256,
  "heartbeat_interval_seconds": 10,

  "connect_timeout": 10.0,
  "forward_timeout": 60.0,
  "max_retries": 3,
  "retry_backoff_base": 1.0,
  "retry_backoff_max": 10.0,
  "keepalive_time_ms": 30000,
  "keepalive_timeout_ms": 10000
}
