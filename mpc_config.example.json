{
  "_comment": "UNFED AI — MPC Node Config (privacy-preserving shard 0). Copy to mpc_config.json and adjust. You need TWO of these: one for role A (entry) and one for role B (peer).",

  "role": "mpc",
  "port": 50061,
  "host": "[::]",
  "advertise": "localhost:50061",
  "registry": "localhost:50050",

  "mpc_role": "A",
  "peer_address": "localhost:50062",

  "shard_index": 0,
  "model_name": "Qwen/Qwen2.5-0.5B",
  "model_type": "qwen2",
  "shards_dir": "shards",
  "manifest_path": "shards/manifest.json",

  "grpc_max_workers": 4,
  "grpc_max_message_mb": 256,
  "heartbeat_interval_seconds": 10,

  "connect_timeout": 10.0,
  "forward_timeout": 60.0,
  "max_retries": 3,
  "retry_backoff_base": 1.0,
  "retry_backoff_max": 10.0,
  "keepalive_time_ms": 30000,
  "keepalive_timeout_ms": 10000
}
